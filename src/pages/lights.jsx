import React from 'react';
import Items from '../components/items';

const Lights = props => {	
	return (
		<div className="content">
			<Items type="lights" />
		</div>
	);
};

export default Lights;
